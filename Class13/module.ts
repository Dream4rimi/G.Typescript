function add(para: number, para2: number): number{
    return para + para2;
};


 function subtract(para: number, para2: number): number{
    return para + para2;
};


let hoga = {
    add,
    subtract
};

export default hoga;