import a from './first.js'

import { bee, cee } from './second.js'

console.log(a + " " + bee + " " + cee);  // Output: my salary 25000



// Notes

// English:
// Native ECMAScript modules (ESM) are a standardized module system for JavaScript that allows for the import and export
// of code between files using import and export statements. They differ from other module systems like CommonJS by
// being natively supported in the browser and having a static structure, which means the module dependencies are 
// resolved at compile time rather than at runtime.

// Urdu:
// Native ECMAScript modules (ESM) JavaScript ke liye aik standardized module system hai jo import aur export statements 
// ka use karke code ko files ke beech import aur export karne ki sahulat deta hai. Yeh dusre module systems jaise ke
// CommonJS se mukhtalif hain kyunke yeh browser mein natively supported hain aur inki structure static hoti hai, yani
// module dependencies ko compile time par resolve kiya jata hai na ke runtime par.